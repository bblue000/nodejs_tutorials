1、语法
标示符：大小写敏感；
常量、变量、函数、“类”的声明：跟java和C基本类似，特别地，声明一个数组[]，一个“匿名”Object{}；
循环语句；
with语句；


2、重要语法
函数，可以：
	|- 仅代表一个方法，执行一段代码
	|- 作为参数，委派
	|- 作为“类”声明


总是可变参数


3、特性：
	1、基于对象：

	2、弱类型：变量类型声明、赋值时自动转换（隐式转换，类型检查）
		var param = 1;
		param = '1';

	3、基于原型：--继承，多态
		由于可以运行时动态改变原型，可以选择'爸妈'....
		由于可以运行时动态改变原型，可以蒙混过关，欺骗虚拟机....

	4、动态



